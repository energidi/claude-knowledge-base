<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Required__c</fullName>
    <defaultValue>false</defaultValue>
    <description
  >Checkbox (Yes/No). When checked, the user must select an ICD-10 code before the Flow screen can advance - leaving the field empty will block navigation. This value overrides the Required setting in Flow Properties when a matching Flow API Name record exists and is active. Defaults to false (optional). ref: ISP-6429</description>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <label>Required?</label>
    <type>Checkbox</type>
</CustomField>
