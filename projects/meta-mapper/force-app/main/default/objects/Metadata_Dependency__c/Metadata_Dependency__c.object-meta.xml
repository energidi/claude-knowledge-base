<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>Represents one metadata dependency discovered during a scan. Records exist only while the scan is active - they are the engine's working state that tracks which components have been found and which still need processing. When the scan completes, all records for that job are serialized to a Salesforce File and deleted, freeing Data Storage. Records for Failed or Cancelled scans remain until the nightly cleanup batch removes them. Do not create or edit records manually. Ref: ISP-6072</description>
    <label>Metadata Dependency</label>
    <nameField>
        <displayFormat>MDN-{0000}</displayFormat>
        <label>Metadata Dependency Name</label>
        <type>AutoNumber</type>
    </nameField>
    <pluralLabel>Metadata Dependencies</pluralLabel>
    <deploymentStatus>Deployed</deploymentStatus>
    <sharingModel>ControlledByParent</sharingModel>
</CustomObject>
